# dipendenze
node_modules/
.pnpm-store/

# fixture temporanei del test della regola anti-dominio-hardcoded
# (packages/shared/src/regola-dominio-hardcoded.test.ts): non dovrebbero mai
# sopravvivere a un run, presenti solo come rete di sicurezza.
**/.tmp-lint-fixture-*.ts

# build
dist/
.next/
.turbo/
out/
build/
*.tsbuildinfo

# ambiente
.env
.env.*.local

# certificati TLS locali (mkcert), specifici della macchina
docker/traefik/certs/*.pem

# dump di backup del database: dati reali, mai versionati
backups/*.dump

# test / coverage
coverage/

# log
*.log
npm-debug.log*
pnpm-debug.log*

# editor / os
.DS_Store
*.swp

# IDE (JetBrains) — configurazione locale, non versionata
.idea/

# Playwright: sessione condivisa tra i test e2e, risultati e tracce
apps/web/e2e/.auth/
apps/web/test-results/
apps/web/playwright-report/
