{
  "name": "gestilab",
  "private": true,
  "packageManager": "pnpm@11.4.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "docker compose -f compose.yaml -f compose.dev.yaml up",
    "dev:local": "turbo run dev",
    "local-prod": "BASE_DOMAIN=gestilab.test docker compose -f compose.yaml -f compose.local-prod.yaml up",
    "db:migrate": "docker compose -f compose.yaml -f compose.dev.yaml exec api pnpm --filter @gestilab/db run migrate",
    "db:seed": "docker compose -f compose.yaml -f compose.dev.yaml exec api pnpm --filter @gestilab/db run seed",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@eslint/eslintrc": "3.3.7",
    "@eslint/js": "9.39.5",
    "@types/node": "^26.6.1",
    "eslint": "9.39.5",
    "globals": "17.12.0",
    "prettier": "^3.9.8",
    "turbo": "^2.11.0",
    "typescript": "6.0.3",
    "typescript-eslint": "8.70.0",
    "vitest": "^5.0.1"
  }
}
